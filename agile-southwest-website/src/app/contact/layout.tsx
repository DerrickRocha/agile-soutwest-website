export const metadata = {
  title: 'Contact',
  description: 'Contact Agile Southwest',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
